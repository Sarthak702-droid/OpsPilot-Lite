# ADR 003: SGLang inference

Serve XiaomiMiMo/MiMo-V2.6-Pro-RL with SGLang behind a private endpoint. The backend uses the OpenAI compatible chat completions API; no application process loads weights. GPU deployment scales independently from the business stack.

