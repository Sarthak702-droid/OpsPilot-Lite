"use client";
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, CartesianGrid } from "recharts";
import type { Signal } from "@/types/api";
export function SignalSummary({ signals }: { signals: Signal[] }) { const data = [{ name: "Inventory", types: ["STOCKOUT_RISK", "OVERSTOCK"] }, { name: "Receivables", types: ["PAYMENT_OVERDUE", "HIGH_VALUE_PAYMENT_PENDING"] }, { name: "Suppliers", types: ["SUPPLIER_DELAY"] }].map(group => ({ name: group.name, count: signals.filter(s => group.types.includes(s.signal_type)).length })); return <div className="chart-placeholder"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={{ top: 10, right: 8, bottom: 0, left: -27 }}><CartesianGrid stroke="#e8edf1" vertical={false} /><XAxis dataKey="name" tickLine={false} axisLine={false} tick={{ fill: "#637083", fontSize: 11 }} /><YAxis allowDecimals={false} tickLine={false} axisLine={false} tick={{ fill: "#637083", fontSize: 11 }} /><Tooltip /><Bar dataKey="count" fill="#145f55" radius={[4, 4, 0, 0]} maxBarSize={45} /></BarChart></ResponsiveContainer></div>; }
